{
  "version": 2,
  "buildCommand": "pnpm install && pnpm -F web build",
  "installCommand": "pnpm install",
  "nodeVersion": "20.x",
  "builds": [
    {
      "src": "apps/web/package.json",
      "use": "@vercel/next@latest",
      "config": {
        "maxLambdaSize": "50mb"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "apps/web/$1"
    }
  ]
}
